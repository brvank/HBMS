//to add mounted in all the async tasks -> added the mounted function to handle
//to add all the state flags inside view models -> handled with the below one
//to centralise the loading behaviour of each activity or fragment -> added a setLoading(boolean) function for it and for the above task
//TODO: to keep all UI data in the view model
//TODO: each view model only associated with one activity (except for any other major requirement)